cmake_minimum_required(VERSION 3.10)

project(Logs)

# Inclua meus arquivos de dependencia
include_directories(./include)

# Adicione seus arquivos-fonte e execute a compilação
add_executable(result main.cpp)

